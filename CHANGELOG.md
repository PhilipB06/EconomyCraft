### Features
- Added search to `/shop`, `/servershop`, and `/orders` (`search <query>`, or the Search button in-GUI).
- Added sorting to `/shop`, `/servershop`, and `/orders` GUIs.
- Added `/worth [<item> [<amount>]]` to check an item's buy/sell price.
- Replaced `/sell`'s subcommands with a GUI.
